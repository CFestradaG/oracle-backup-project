# =============================================================================
# Archivo de variables de entorno - EJEMPLO
# Copiar a ".env" y ajustar los valores antes de ejecutar docker compose
# IMPORTANTE: No subir el archivo ".env" a GitHub (está en .gitignore)
# =============================================================================

# Contraseña para los usuarios SYS, SYSTEM y PDBADMIN de Oracle XE
ORACLE_PWD=Oracle123#
